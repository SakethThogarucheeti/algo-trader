# This module is superseded by the trading.di.providers package.
# It is kept only so that any import of `trading.di.providers` (the module)
# continues to resolve — Python prefers packages over modules of the same name
# when both exist, so this file will NOT be imported once the package directory
# is present.  It is safe to delete this file.
